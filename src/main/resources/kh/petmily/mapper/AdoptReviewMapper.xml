<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kh.petmily.mapper.AdoptReviewMapper">

    <!--=======BasicMapper 메소드=======-->
    <select id="selectByPk" resultType="kh.petmily.domain.adopt_review.AdoptReview">
        select *
        from BOARD
        where BNUMBER = #{pk}
    </select>

    <insert id="insert">
        insert into BOARD(BNUMBER, MNUMBER, KINDOFBOARD, TITLE, CONTENT, IMGPATH, VIDEO, WRTIME, CHECKPUBLIC)
        values (#{bNumber}, #{mNumber}, #{kindOfBoard}, #{title}, #{content}, #{imgPath}, null, SYSDATE, #{checkPublic})
    </insert>

    <update id="update">
        update BOARD
        set TITLE = #{title}, CONTENT = #{content}, CHECKPUBLIC = #{checkPublic}, WRTIME = SYSDATE
        <if test='imgPath != null '>
            ,IMGPATH = #{imgPath}
        </if>
        where BNUMBER = #{bNumber}
    </update>

    <delete id="delete">
        delete from BOARD
        where BNUMBER = #{pk}
    </delete>
    <!--=======BasicMapper 메소드=======-->

    <select id="selectCount" resultType="int">
        select count(*)
        from BOARD
        where KINDOFBOARD = #{kindOfBoard}
    </select>

    <select id="selectIndex" resultType="kh.petmily.domain.adopt_review.AdoptReview">
        select * from
        (select ROWNUM as NUM, A.* from
        (select * from BOARD
        where KINDOFBOARD = #{kindOfBoard}
        order by BNUMBER desc) A)
        where NUM between #{start} and #{end}
    </select>

    <select id="selectName" resultType="java.lang.String">
            select M.NAME
            from BOARD B inner join MEMBER M
            on B.MNUMBER = M.MNUMBER
            where B.BNUMBER = #{pk}

    </select>
</mapper>